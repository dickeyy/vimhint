import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Global hotkey to slide the vim cheatsheet sidebar in/out.
    /// No default binding — configure at runtime via KeyboardShortcuts preferences.
    static let toggleSidebar = Self("toggleSidebar")
}
